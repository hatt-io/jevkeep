{
  "name": "jevkeep",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Codex plugin that preserves useful conversation excerpts alongside the summary after context compaction.",
  "license": "MIT",
  "author": "hatt-io",
  "homepage": "https://github.com/hatt-io/jevkeep#readme",
  "repository": { "type": "git", "url": "git+https://github.com/hatt-io/jevkeep.git" },
  "bugs": { "url": "https://github.com/hatt-io/jevkeep/issues" },
  "keywords": ["codex", "compaction", "context", "jev", "plugin"],
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "tsc --noEmit && node scripts/build.mjs",
    "install:personal": "node scripts/install.mjs"
  },
  "dependencies": { "proper-lockfile": "4.1.2" },
  "devDependencies": {
    "@types/node": "22.20.3",
    "@types/proper-lockfile": "4.1.4",
    "esbuild": "0.28.2",
    "typescript": "7.0.2"
  }
}
